# Series compose mode

`living_body_ask` routes dense effort/HR questions to `child_tool: series` when children declare `series_tool`.
Response includes `child_tool_mode`. Operators also see a local stderr JSON log (no PHI) when series is selected.
